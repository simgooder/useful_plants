{{#each plantsData}}

    <div style="margin-bottom: 2em; margin-top: 2em; border-bottom: 1px solid grey; padding-bottom: 2em;">

        <img src="{{image}}" alt="">

        <br>
        <strong>{{name}}</strong>
        <br>
        {{title}}

        <br>

        <a href="{{url}}">
            {{url}}
        </a>

        <p>
            {{summary}}
        </p>

    </div>

{{/each}}
